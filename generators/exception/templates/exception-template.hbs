import { HttpException, HttpStatus } from '@nestjs/common';

export class {{upperCase exception}}_EXCEPTION extends HttpException {
  constructor() {
    super(
      '{{upperCase exception}}',
      HttpStatus.BAD_REQUEST,
    );
  }
}